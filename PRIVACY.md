# RePrompt 隐私政策

**最后更新：2026年5月9日**

## 数据收集

RePrompt 不收集、存储或传输任何用户个人数据、浏览历史或设备信息。

## 数据处理

- 插件不追踪用户的浏览行为
- 插件不采集任何网站的访问记录
- 插件不在用户设备上存储 Cookie 或追踪标识

## 图片分析流程

当用户主动点击「破解prompt」按钮时，插件会对所选图片执行以下处理流程：

### 1. AI 视觉分析

将原始图片发送至 AI 视觉模型，生成逆向提示词（包含场景描述、标签、结构化 prompt 和自然语言 prompt）。

- 默认模式：图片经 Supabase Edge Function 代理转发至 mimo Vision API，API Key 存储在服务端
- 自定义模式：图片直接发送至用户配置的 AI 接口

### 2. 向量检索相似提示词（两次调用）

- **图片向量检索**：将原始图片发送至豆包 Embedding API 生成图片向量，查询 Supabase 数据库匹配相似提示词
- **文本向量检索**：将第 1 步的分析结果文本发送至豆包 Embedding API 生成文本向量，再次查询 Supabase 数据库匹配相似提示词

两次检索结果合并去重后，作为参考提示词返回给用户。

### 3. RAG 增强生成（可选）

若检索到相似提示词，将原始图片与参考提示词一同发送至 AI 视觉模型，生成更精准的逆向提示词。

## 数据传输汇总

| 传输目标 | 传输内容 | 用途 |
|---------|---------|------|
| Supabase Edge Function | 原始图片 | 代理调用 mimo Vision API（仅默认模式） |
| mimo Vision API / 用户自配 API | 原始图片 | AI 视觉分析 + RAG 增强生成 |
| 豆包 Embedding API | 原始图片、分析结果文本 | 生成向量用于相似提示词检索 |
| Supabase 数据库 | 文本向量 | 查询相似提示词（不传输图片） |

以上数据仅用于当次分析请求，不会被存储或用于其他目的。

用户可自行配置其他 AI 服务提供商，此时视觉分析数据将直接发送至用户指定的接口地址，不经过 Supabase 代理。

## 权限说明

- `activeTab` — 仅在用户主动交互时读取当前标签页的图片信息
- `<all_urls>` — 用于在任意网页上显示浮动分析按钮
- `sidePanel` — 用于展示分析结果的侧边栏界面
- `storage` — 用于在本地保存用户的 API 配置和分析历史（最多 50 条，纯本地）

## 联系方式

如有隐私相关问题，请通过 GitHub Issues 反馈。